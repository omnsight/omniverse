# Omniverse

## 快速开始

### 启动前端开发服务器

```bash
npm install
npm run dev
```

前端将在 http://localhost:5173 上运行。

### 构建生产版本

```bash
npm run build
```

构建的静态文件将位于 `dist` 目录中。

## 调试命令z

### 前端调试

```bash
# 运行 TypeScript 类型检查
npx tsc --noEmit

# 运行 ESLint 代码检查
npm run lint

# 运行所有测试
npm test
```
